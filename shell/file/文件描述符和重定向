文件描述符是和输入输出相关的整数，用来跟踪已经打开的文件，最常见的文件描述符是stdin,stdout,stderr。我们可以将某个文件描述符中的内容重定向到另一个文件描述符中。

文件描述符是和打开的文件或者是文件流相关的整数。下面的是系统保留的文件描述符。
0————stdin（标准输入）
1————stdout（标准输出)
2————stderr（标准错误）

>和>>的区别,前者会清空文件，后者是追加。

<从文件中读取到标准输入。>截断写入，>>追加写入。

exec 3<input.txt
cat < &3
使用自定义文件描述符